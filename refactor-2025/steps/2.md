# 步骤2: 分析数据库schema

## 任务
从migration文件提取数据库字段信息

## 操作
1. list_dir cloudflare/worker-router/migrations
2. grep "CREATE TABLE" 找所有表
3. 读取关键表的字段：users, clients, tasks, timesheets
4. 生成 standards/db-schema.json

## 输出
standards/db-schema.json:
```json
{
  "users": ["user_id", "username", "email", ...],
  "clients": ["client_id", "client_name", ...],
  "tasks": ["task_id", "title", "status", ...],
  ...
}
```

logs/2.txt:
```
db-schema.json 完成
表: users clients tasks timesheets leaves receipts payroll
```

## 完成
更新 progress.md: 当前步骤=3
输出: ✅ 2/? → 3

