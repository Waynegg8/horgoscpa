-- ================================================================
-- 添加時間戳記欄位 Migration
-- 檔案: 007_add_timestamps.sql
-- 日期: 2025-10-25
-- 描述: 為現有資料表添加 created_at 和 updated_at 欄位
-- ================================================================

-- 注意：所有相關表已經包含 created_at 和 updated_at 欄位
-- 此遷移標記為已執行，但不執行任何操作
SELECT 'Timestamp columns already exist in all tables' AS status;

-- ================================================================
-- Migration 完成
-- ================================================================
